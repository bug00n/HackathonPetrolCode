[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B"]

[tool.pytest.ini_options]
testpaths = ["global_tests", "tests"]
pythonpath = ["."]
markers = [
    "real_data: интеграционные тесты на реальных данных (медленные)",
]