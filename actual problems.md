# Актуальные проблемы проекта

Последнее обновление: 2026-09-22. Текущий результат финализации и evidence: [release/RELEASE_STATUS.md](release/RELEASE_STATUS.md). Старые записи ниже сохраняют историю, а не отменяют последнее подтверждение.

Это постоянный журнал найденных ошибок, расхождений с ТЗ и планом,
существенных ограничений демонстрации и незавершённых проверок.
Запись в журнале сама по себе не означает, что проблема исправлена.

## Правила ведения

- Добавлять новые существенные находки по мере работы, а не оставлять их только в чате.
- Сохранять идентификаторы записей; дополнять существующую запись вместо создания дубля.
- Разделять подтверждённые ошибки, ограничения и предположения, требующие проверки.
- Для ошибки указывать условие воспроизведения, фактическое и ожидаемое поведение,
  затронутый код и критерий закрытия.
- После исправления обновлять статус, дату и результат проверки. Закрытые записи
  сохранять, чтобы оставалась история решений.
- Прохождение общих тестов не считать подтверждением исправления конкретной ошибки
  без проверки её исходного случая.

Приоритеты: `P1` - исправить до демонстрации; `P2` - существенная проблема или пробел,
который нужно устранить либо явно учесть при защите.

## Реестр

| ID | Приоритет | Тип | Статус | Проблема |
| --- | --- | --- | --- | --- |
| AP-001 | P1 | Ошибка | Закрыта 21.09 | Отсутствующее значение T95/цетанового числа превращается в ноль |
| AP-002 | P1 | Пробел проверок | Закрыта 21.09 | T95 и цетановое число не ограничивают допустимость рецептуры |
| AP-003 | P2 | Расхождение с планом | Закрыта 21.09 | History UI не показывает полноценный результат прогноза |
| AP-004 | P1 | Ошибка UI | Закрыта 22.09; main-thread queue и lifecycle | Асинхронный результат обращается к уничтоженному экрану |
| AP-005 | P2 | Ошибка оценки | Закрыта 22.09; diagnostic маркировка и held-out test | Validation-отчёт оценивает artifact на данных его финального обучения |
| AP-006 | P2 | Ошибка объяснения | Закрыта 21.09 | Recommend ошибочно объясняется нарушением ограничений |
| AP-007 | P2 | Ограничение демо | Закрыта 22.09; tradeoff regression/evidence | Сценарии слабо демонстрируют конфликт критериев оптимизации |
| AP-008 | P2 | Пробел проверки | Проверена 22.09; test 31 819 целей, baseline не улучшен | Качество ML на полном реальном датасете не подтверждено этой ревизией |
| AP-009 | P2 | Пробел демонстрации | Закрыта 22.09; прогресс, отмена и частичный экспорт | Нет штатного расчёта по произвольному историческому интервалу |
| AP-010 | P1 | Ошибка UX / производительности | Закрыта 22.09; 2 точки 17 с, 7 точек 48 с | Старт и переходы UI блокируются обработкой полной истории |
| AP-011 | P1 | Несовместимость поставки | Исправлена 21.09; dataset пересобран и manifest закреплён | Автоматические dataset/model не образуют рабочий комплект |
| AP-012 | P1 | Пробел поставки | Финальная сборка 22.09; см. RELEASE_STATUS | source.zip от 18.09 устарел и не содержит моделей |
| AP-013 | P1 | Ошибка UI | Исправлена 21.09; regression test добавлен | Неверная дата обрывает worker без сообщения в результате |
| AP-014 | P1 | Пробел запуска | Закрыта 22.09; EXE-first инструкция | README не даёт короткий буквальный маршрут первого запуска |
| AP-015 | P2 | Понятность UI | Исправлена 21.09; основные поля переведены и pinned | Технические поля и разрозненные формы вместо операторского маршрута |
| AP-016 | P2 | Контекст UI | Закрыта 22.09; общий dataset/time форм | Начальное время позже телеметрии; экраны используют разные состояния |
| AP-017 | P2 | Точность отображения | Исправлена 21.09; доли и масса показываются точно | Рецептура округлена до целых процентов и отличается от рассчитанной |
| AP-018 | P2 | Журнал / экспорт | Закрыта 22.09; регрессия экспорта пройдена | UUID и raw JSON; нет единого экспорта текущего результата всех режимов |
| AP-019 | P2 | Граница режима | Исправлена 21.09; экран явно подписан | Hybrid — только условная оценка серы, не полный паспорт или оптимизация цепочки |
| AP-020 | P1 | Материалы сдачи | Закрыта 22.09; новые DOCX 7 стр. / PPTX 12 слайдов | DOCX/PPTX относятся к старой версии; часть метрик уже отличается |
| AP-021 | P1 | Пробел приёмки | Закрыта 22.09; portable EXE все режимы + Tk | Чистая установка и offline-показ финального комплекта не подтверждены |
| AP-022 | P2 | Пробел визуальной приёмки | Проверена 22.09; кнопки над результатом, 2 ряда | Размер окна, DPI и доступность всех элементов требуют финального прогона |
| AP-023 | P2 | Ограничение интерактивного демо | Закрыто 22.09, v1.1.0 | Добавлен синтетический what-if: свойства, запасы, присадка, валидация, пересчёт и сброс |
| AP-024 | P1 | Доступ к поставке | Публикация проверена 22.09, 12:52 МСК; доступ жюри не проверен | v1.1.0 опубликован с полным ZIP; приватность требует доступа жюри либо прямой передачи архива |

Полный продуктовый аудит, доказательства, критерии исправления и порядок финализации:
[FINAL_READINESS_REVIEW.md](FINAL_READINESS_REVIEW.md). Он не означает, что перечисленные
изменения уже реализованы. Проверенная база: `dev`, `88e317e21f593626315dc3b44c5cae56bdedc849`.

## AP-001. Пропуски свойств превращаются в ноль

Обнаружено и воспроизведено: 2026-09-14.

Код: [source/agents/effects.py](source/agents/effects.py), `_weighted_metric()`;
для сравнения: [source/ml/blending.py](source/ml/blending.py), `calculate_mass_blend()`.

Условие воспроизведения: взять `blend_risk` и у обоих компонентов оставить объект
`t95`, но задать `value=None`, `lower=None`, `upper=None`, `interval_kind="none"`.
Аналогичный случай воспроизводится для `cetane_number`.

Фактический результат: выражение `estimate.value or 0.0` заменяет отсутствующее
значение нулём. Цикл возвращает `recommend`, quality-agent имеет статус `ok`,
обязательное свойство равно `0.0`, blocking issues отсутствуют.
Второй расчёт смеси, `calculate_mass_blend()`, на тех же данных сохраняет `None`.

Ожидается: неизвестное значение свойства компонента с положительной долей остаётся
неизвестным. Если свойство отсутствует у всех доступных компонентов, цикл возвращает
`abstain`, а причина объясняет нехватку обязательных данных.

Критерий закрытия: проверить отдельно отсутствие объекта свойства и `value=None`
в существующем объекте для T95 и цетанового числа; результаты обоих расчётов смеси
должны совпадать. Нулевая доля компонента с отсутствующим свойством не должна
ошибочно делать расчёт неизвестным.

Исправлено 21.09: неиспользуемая `_weighted_metric()` удалена; активный расчёт
сохраняет неизвестное значение. Параметризованный тест проверяет оба вида пропуска
для T95/CN, согласованность двух расчётов и `abstain` цикла.

## AP-002. T95 и цетановое число не участвуют в проверке пределов

Обнаружено и воспроизведено: 2026-09-14.

Код: [blend_risk.json](config/scenarios/blend_risk.json), а также
[blend_normal.json](config/scenarios/blend_normal.json) и
[blend_missing.json](config/scenarios/blend_missing.json);
[source/constraints.py](source/constraints.py), `check_constraints()`;
[source/ui.py](source/ui.py), `_constraint_rows()`.

Условие воспроизведения: в `blend_risk` задать обоим компонентам `T95=500`
с верхней оценкой `505` и цетановое число `20` с верхней оценкой `20`.

Фактический результат: цикл возвращает `recommend`. В выбранном варианте есть
проверки только серы и запасов компонентов. Для T95 и цетанового числа вычисляются
значения, но ограничения для них в сценариях отсутствуют; UI показывает их как
«Оценено» зелёным цветом.

Это не утверждение о конкретных промышленных пределах. Проблема в том, что система
вообще не может проверить допустимость этих обязательных свойств в рамках демо.

Критерий закрытия: явно задать модельные пределы выбранных сценариев, подписать их
как допущения и включить в общий фильтр допустимости. Проверить выход за каждый
предел и случай отсутствующей оценки. UI должен отличать наличие рассчитанного
значения от успешной проверки ограничения.

Исправлено 21.09: сценарные ограничения T95/CN включены в единый hard-filter;
они обозначены как модельные допущения. Проверки Stage 1/4/UI покрывают выход за
пределы и неизвестное качество; промышленной сертификацией это не является.

## AP-003. History UI не показывает полноценный прогноз

Обнаружено и воспроизведено без открытия Tk: 2026-09-14.

Код: [source/ui.py](source/ui.py), `_run_history_from_ui()`,
`_render_history()` и `history_smoke_snapshot()`;
тест: [global_tests/test_ui.py](global_tests/test_ui.py).

Условие воспроизведения: передать fixture dataset и point-model из
[global_tests/test_stage7_history_serving.py](global_tests/test_stage7_history_serving.py)
с `as_of=2026-01-15T09:00:00Z`.

Фактический результат: backend вычисляет серу `8.8 мг/кг`, верхняя оценка отсутствует,
а quality-agent возвращает issue `UNCERTAINTY_UNAVAILABLE`. Экран сохраняет только
`status`, `model_id`, `reason_codes`, `explanation` и `run_id`. Сам прогноз серы,
подробности issues и ссылка на журнал не выводятся. Для этого fixture статус
`abstain` и текст объяснения также не показывают численное значение прогноза.

Критерий закрытия: выводить point/upper sulfur, состояние uncertainty, issues,
reason codes, model ID и доступный путь к журналу. Проверять эту проекцию тем же
кодом, который использует настоящий экран. Отсутствие upper должно быть показано
явно; history остаётся forecast-only.

Исправлено 21.09: экран использует `format_history_replay_view()` для полной
проекции, включая point/upper, issues, причины и журнал; тесты проверяют тот же
formatter и forecast-only статус.

## AP-004. Смена вкладки ломает завершение history-расчёта

Обнаружено и воспроизведено на настоящем Tk: 2026-09-14.

Код: [source/ui.py](source/ui.py), `show_page()`, `_clear_body()`,
`_run_history_from_ui()` и `_history_done()`.

Условие воспроизведения: открыть «Историю», запустить расчёт, перейти в «Журнал»
до прихода результата. В проверке был воспроизведён тот же порядок событий:
создание history-виджета, смена страницы, вызов обработчика завершения.

Фактический результат: смена страницы уничтожает поле вывода, но callback сохраняет
ссылку на него. `_history_done()` вызывает `delete()` у удалённого виджета и получает
`TclError: invalid command name`.

Критерий закрытия: сохранять состояние и результат запроса независимо от виджетов;
обновлять только существующий актуальный экран. Проверить переход на другую вкладку
во время расчёта, возврат на «Историю» и повторные запуски с разным порядком завершения.

Реализация исправлена 21.09: удалён перекрывавший новый экран старый обработчик;
результат хранится отдельно от виджетов, устаревшие request ID игнорируются,
удалённый виджет не обновляется. Интерактивный Tk-сценарий после интеграции
ещё не повторён, поэтому запись не закрыта.

Дополнение 21.09, ревью готовности: проблема сохраняется в hybrid-панели.
На настоящих скрытых Tk-виджетах выполнено: «Рассчитать hybrid», через 50 мс
переход в журнал, через 300 мс контролируемый ответ backend. Получен
`TclError: invalid command name` в `source/ui.py:1238/1249` при обновлении
уничтоженного Text. Backend-ответ в этой lifecycle-проверке подменён задержанным;
реальный hybrid отдельно успешно рассчитан на актуальном prepared dataset.
History-защита не объявляется сломанной этим другим случаем; общий пункт не закрыт.
Приоритет повышен до P1 для финальной демонстрации. Проверить также закрытие
диалогов v2/action/LIMS и повторные запросы; эти частные случаи пока не воспроизведены.

## AP-005. Validation-отчёт не является независимой оценкой artifact

Обнаружено и подтверждено трассировкой обучения: 2026-09-14.

Код: [source/ml/train.py](source/ml/train.py), `train_model()`;
[source/main.py](source/main.py), `evaluate_command()`;
[source/ml/evaluate.py](source/ml/evaluate.py), `evaluate_model()`.

Условие воспроизведения: выполнить `train`, затем `evaluate --split validation`
для созданного artifact и того же prepared dataset.

Фактический результат: после выбора модели финальный predictor обучается на
`train + validation`. Команда evaluation затем использует те же validation-строки.
В проверке все 26 строк validation-отчёта входили в финальную обучающую выборку.

Такие метрики нельзя трактовать как независимую проверку качества. Это замечание
не означает, что test использовался для выбора модели: такой утечки в данном
случае не установлено.

Критерий закрытия: различать validation-метрики выбора модели до финального refit
и оценку сохранённого artifact. Для artifact, обученного на train + validation,
либо запрещать выдачу validation как независимой оценки, либо явно подписывать
пересечение с обучением. Held-out оценка должна использовать отдельные данные.

## AP-006. Объяснение recommend может противоречить проверкам

Обнаружено и воспроизведено: 2026-09-14.

Код: [source/explain.py](source/explain.py), `build_explanation()`;
[source/ui.py](source/ui.py), `recommendation_to_view()`.

Условие воспроизведения: взять `blend_normal`, установить текущую рецептуру
`A=1.0, B=0.0` и порог существенности `cost_proxy=0.005`.

Фактический результат: baseline допустим, статус равен `recommend`, причина равна
`MATERIAL_IMPROVEMENT`. При этом объяснение пишет «текущий режим не проходит
ограничения», а UI пишет «Текущая рецептура нарушает ограничение по сере».
В тех же результатах проверка серы baseline имеет статус `pass`.

Критерий закрытия: строить текст из фактической причины выбора и результатов checks.
Отдельно проверить рекомендацию из-за нарушения качества и рекомендацию из-за
существенного улучшения при допустимом baseline. Во втором случае объяснять,
какой критерий улучшился.

Исправлено 21.09: объяснение и UI различают нарушение baseline и улучшение
допустимого baseline. Исходный случай закреплён регрессионным тестом.

## AP-007. Демо слабо показывает конфликт критериев

Обнаружено по конфигурации: 2026-09-14. Это ограничение демонстрации, а не ошибка
арифметики или нарушение запрета на реальные управляющие воздействия.

Код: [blend_risk.json](config/scenarios/blend_risk.json);
[source/agents/effects.py](source/agents/effects.py), `calculate_blend_metrics()`.

У обоих компонентов индекс риска равен `0.0`, а выпуск задан одной и той же массой
партии для всех рецептур. Поэтому сценарий не показывает содержательный компромисс
по риску и производительности: выбор фактически определяется серой, запасами,
стоимостным прокси и политикой существенности изменения.

Критерий закрытия: добавить явно синтетический пример с различающимися критериями
и показать, почему выбранный допустимый вариант предпочтительнее альтернатив.
Жёсткие ограничения должны сохранять приоритет. Не выдавать модельные прокси
за измеренную надёжность оборудования или реальную экономию.

## AP-008. Нет подтверждения качества на полном реальном датасете

Зафиксировано как граница проведённой проверки: 2026-09-14.

В этой ревизии обучение, evaluation и history-serving проверены на fixtures.
Качество ML на полном реальном датасете не оценивалось. Успешные CLI-команды
и unit tests подтверждают работоспособность пути, но не точность реального прогноза.

Критерий закрытия: выполнить воспроизводимый прогон подготовки данных, обучения
и оценки на отложенном реальном периоде; сохранить model/dataset IDs, версии
окружения, отчёт и residuals. Сопоставить модель с baseline, показать ошибки около
предела серы и пропущенные превышения. Зафиксировать результаты и ограничения
в этом журнале со ссылками на полученные материалы.

Дополнение 21.09, ревью готовности: полный пакет подготовлен отдельно в
`../output/readiness-2026-09-21/prepared/66bdfcbb23b4` (189217 telemetry,
301901 quality, 11 issues). На нём выполнены отдельные history/hybrid/v2
расчёты и пересчитана LIMS-коррекция. Результаты сохранены в
`../output/readiness-2026-09-21/recovered_checks.json` и приведены в
`FINAL_READINESS_REVIEW.md`. Полный независимый исторический аудит всех режимов
и периодов этим не выполнен; AP-008 остаётся открытой.

## AP-009. Выбор исторического интервала для демонстрации

Обнаружено 21.09.2026 при разборе Q&A 18.09; подтверждено кодом и CLI `--help`.
Источник: видеозапись Q&A 18.09, примерно 08:15–08:35; полный разбор и
расшифровка находятся в `../output/qa-2026-09-18/`.

Эксперт предложил несколько тестовых срезов и возможность задавать период
расчёта. Это пожелание к демонстрации, не доказательство ошибки ML.

Код: `source/main.py`, `evaluate_command()` и аргументы команд `evaluate`/`replay`.
Сейчас `evaluate` принимает только `--split train|validation|test`, а `replay`
принимает одну точку `--at`; выбор начала/конца исторического интервала отсутствует.

Ожидается: воспроизводимый запуск на заданном интервале доступных данных с
явными датами и отчётом о количестве точек, прогнозах/отказах и покрытии.
Критерий закрытия: продемонстрировать минимум нормальный и проблемный
исторические срезы через одну документированную команду; сохранить необходимую
предысторию признаков, не смешивать обучающие интервалы с независимой оценкой.
Полный реальный прогон и достоверность его метрик отдельно учитываются в AP-008.

## AP-010. Зависания старта и навигации

21.09 воспроизведено: обычный старт «Обзора» и переходы к стадиям выполняют чтение
полной истории в главном Tk-потоке. Стек: `source/ui.py`, `_render_stage_cards` /
`_render_stage_page` → `source/ui_data.py` → pandas read_csv/обработка дат/наблюдений.
Минимальный Tk работает; старт через журнал создаёт приложение примерно за 0,108 с.
Ожидается отзывчивая оболочка с видимым состоянием загрузки. Закрытие: холодный
старт, переходы и полная история проверены в настоящем GUI без длительной блокировки.
Точные критерии и границы замера — в разделе AP-010 итогового ревью.

## AP-011. Нет единого совместимого default-комплекта

21.09 воспроизведено: UI выбирает a2fe8577752a или aacc7c1ab3d9; оба не соответствуют
текущим runtime/tags/rules. После prepare новый набор 66bdfcbb23b4 работает с
sulfur-upper-33efba3c3141, но не со старым default sulfur-upper-e753ac99b59e.
Автовыбранный v2 experimental требует отсутствующий `source.ml.experimental`;
action-shadow отвергает новый dataset ID. Код: `source/ui_data.py`, discovery;
`source/ui.py`, defaults диалогов. Ожидается выбор из проверенного release-manifest,
а не по имени/mtime. Закрытие: все заявленные формы работают с defaults в чистой поставке.

## AP-012. Архив не обеспечивает заявленные режимы

21.09 проверен `../output/jury-finalization-2026-09-18/source.zip`: 129 записей,
исходный data.rar присутствует, models/prepared отсутствуют; 33 общих текстовых
файла отличаются от текущего checkout. При распаковке acceptance проходит 5/5,
verify-model-freeze падает на отсутствующем metadata.json. Ожидается новый полный
комплект из одной версии. Закрытие: все заявленные режимы воспроизводятся из
архива без локальных файлов автора и без обязательного обучения перед показом.

## AP-013. Неверный timestamp оставляет расчёт без ответа

21.09 на настоящем Tk: History/ML → дата `21.09.2026` → расчёт. Worker падает:
`ui_history_snapshot` повторно вызывает `_parse_as_of` в обработчике ошибки
(`source/ui_data.py:364`). Ожидается сообщение о формате и завершённое состояние
ошибки, не старый результат. Закрытие: неверная/пустая дата, отсутствие timezone,
повтор после исправления проверены; нет фонового необработанного исключения.

## AP-014. Неочевидный первый запуск

README начинает с истории разработки; первый блок создаёт .venv, но не активирует
её и не вызывает её python явно, затем требует placeholder dataset/model ID.
Есть Bash-переносы в примерах, launcher отсутствует. Ожидается короткий отдельный
Windows-маршрут с подготовленным комплектом и ожидаемым первым экраном.
Закрытие: человек без контекста выполняет инструкцию буквально, без поиска ID,
редактирования JSON и переключения на training commit.

## AP-015. Технические формы вместо понятного маршрута

UI показывает Prepared dataset, Forecast artifact, As of, fresh/stale и raw-коды;
исследования находятся в раскрывающемся блоке обзора. Источники: `source/ui.py`,
формы и formatter. Ожидаются русские названия, объяснения режимов, проверенные
примеры и раскрываемые технические подробности. Закрытие: пользователь без DESIGN
понимает данные, статус, границу результата и следующий шаг.

## AP-016. Разные состояния и неудачный момент по умолчанию

21.09 default_as_of = 2026-08-11T06:54:04Z, телеметрия заканчивается 06.08 в 21:00Z.
АВТ сразу имеет 21 устаревший сигнал, гидроочистка — 10; страницы и диалоги выбирают
разные datasets/timestamps. Источник: `source/ui_data.py`, `_default_as_of`, defaults
в `source/ui.py`. Ожидаются общий контекст и объяснение покрытия источников.
Закрытие: переходы сохраняют выбранное состояние; хвост без телеметрии объяснён;
старый несовместимый набор не представлен как готовый текущий комплект.

## AP-017. Точность рецептуры в UI

`source/ui.py`, `_recipe_table`, формат `.0f`: 89,1/9,9/1% превращаются в 89/10/1%.
Ожидается отображение именно рассчитанных массовых долей с достаточной точностью.
Закрытие: пять сценариев, экран, экспорт и презентация согласованы; по показанным
долям можно воспроизвести результат. Исходные hard checks не округлять.

## AP-018. Журнал и экспорт ориентированы на разработчика

`source/ui.py`, `_render_journal`: список UUID и raw result.json без операторской
сводки. `export_result` сохраняет model-demo self._result; нет общего маршрута
экспорта текущего history/hybrid. Полный ZIP существует в CLI. Ожидаются понятные
названия запусков и экспорт именно показанного результата. Закрытие: из любого
заявленного результата открывается его журнал/полный след; отмена и ошибка записи
обработаны. Это пробел пользовательского маршрута, не отсутствие журналирования.

## AP-019. Ограниченная семантика hybrid

`source/ui_data.py`, `ui_hybrid_snapshot`: условное смешение по сере, не полная
оптимизация цепочки. На проверенной точке верхняя сера смеси 16,57166 мг/кг,
status ready при constraint fail. Ожидается ясное разделение готовности расчёта,
пригодности смеси и неподтверждённых свойств/потоков. Закрытие: UI/описание не
выдают результат за полный товарный паспорт; fail/unknown объяснены отдельно.

## AP-020. Устаревшие материалы сдачи

DOCX/PPTX от 18.09 относятся к d0bbf23, слайд 10 показывает UI от 11.09.
Пять demo-чисел остаются верны; новые LIMS-метрики отличаются (см. итоговое ревью).
Ожидается одна release-версия в архиве, README, описании и презентации, с
проверяемым происхождением чисел. Закрытие: обновлены commit/команды/screenshots,
старые метрики либо явно архивные, либо заменены отчётом итогового комплекта;
последние условия Q&A и границы возможностей отражены.

## AP-021. Чистый offline-прогон не выполнен

Это пробел проверки, не доказанный сетевой дефект. Запуски текущего аудита и
распакованного старого ZIP использовали установленное окружение автора.
Ожидается отдельная проверка конечного комплекта на новой папке/профиле/машине,
без интернета и доступа к рабочим каталогам. Закрытие: записаны среда, команды,
входы и результаты; установка без сети заявляется только при отдельном обеспечении.

## AP-022. Визуальная приёмка размеров и DPI

Стартовое geometry 1536×1024 превышает логическую высоту экрана проверки 960;
есть широкие таблицы и длинные поля. Полная матрица DPI/разрешений не выполнена;
часть диагностических снимков ограничена кадрированием и вмешательством в навигацию,
по ним нельзя приписывать приложению любую обрезку. Ожидается доступность всех
кнопок/причин/таблиц на целевом ноутбуке и проекторе. Закрытие: весь маршрут
проверен на целевых разрешениях и масштабах, сохранены корректные новые снимки.

## Что уже проверено (архив и интеграция)

Результаты повторной проверки 2026-09-14:

- `pytest -q`: 109 тестов прошли.
- `ruff check .`, `ruff format --check .`, `mypy source`: успешно.
- `validate-stage0`: 5 сценариев, 3 model-demo fixtures, 170 тегов.
- `accept-stage6`: успешно; `blend_normal=hold`, `blend_risk=recommend`,
  `blend_missing=abstain`; обязательные journal-файлы присутствуют.
- Train/evaluate и history-serving работают на проверенных fixtures.
- Настоящий Tk запускается вне песочницы. Ошибка `Can't find a usable init.tcl`
  внутри песочницы оказалась ограничением проверки и не считается открытой
  неисправностью установки Python. AP-004 подтверждена отдельно после успешного запуска Tk.

Это архивная проверка от 14.09; актуальные статусы указаны в реестре выше.

Проверка интеграции `dev` от 21.09: `pytest -q` — 163 passed;
`ruff check .`, `ruff format --check .`, `mypy source` — успешно;
`validate-stage0` — 7 сценариев, 5 fixtures, 170 тегов;
`verify-model-freeze` — 2 модели и 2 отчёта;
`acceptance` — 5 эпизодов (`hold`, 3 `recommend`, `abstain`).
Это не снимает AP-005/AP-007/AP-008 и не доказывает промышленную пригодность.
## Дополнение от 20.09.2026

Ниже сохранены результаты отдельной проверки ветки. Их статусы требуют сверки
с текущим `dev`; пункты не считаются автоматически исправленными.

## Active blockers

- `actual problems.md` был восстановлен в `dev` при интеграции 21.09.
- Frozen model artifacts и отчёты сейчас присутствуют локально: `verify-model-freeze`
  успешно проверяет две модели и два отчёта. На другом checkout требуются Git LFS
  и проверка доступности тех же файлов.
- Архивная запись про единственный `data/processed/d175aedffaba` более неактуальна:
  локально присутствуют `a2fe8577752a` и `aacc7c1ab3d9`. Совместимость каждого
  подготовленного набора с текущими конфигурациями надо проверять перед использованием.
- Диагностика на устаревшем dataset должна возвращать ошибку совместимости до ML-анализа;
  текущие CLI-команды выполняют такую проверку. Отдельный реальный прогон не проведён.
- Для атомарной публикации prepared dataset добавлен ограниченный retry на Windows
  `PermissionError`; интеграционный тест Stage 2 прошёл.
- Current P8/F19 historical action artifacts remain shadow-only. `supports_actions=true` is blocked until engineering bounds, step/unit evidence, temporal gates, shadow replay, and technologist approval all pass.

## Current implementation status

- Model-demo acceptance is working: `blend_normal=hold`, risk scenarios recommend, and missing quality abstains.
- History forecast is read-only unless a verified action artifact is supplied.
- Tkinter UI is now stage-aware: `avt`, `hydrotreating`, `blend`, `history`, and `journal` have real screens or read-only projections instead of disabled placeholders.
- Observed telemetry quantiles are research context only and must not become engineering limits.
- `ht:P8` and `ht:F19` are the only first production-action candidates; `ht:T11` and `ht:F26` remain context-only.

## Fixed in current working tree

- Prepared dataset publication now retries transient Windows `PermissionError` during atomic directory replace.
- CLI commands that consume prepared data now reject stale config/tag/rules hashes before ML diagnostics/training/replay.
- `verify-model-freeze` now understands production action artifacts and checks controls, horizons, fingerprints and gate report hashes.
- `replay` has an optional `--action-model` path. Without it, history remains forecast-only and abstains with `ACTION_MODEL_UNAVAILABLE`.
- Tkinter history replay has a separate optional verified action artifact field and a dedicated `История/ML` screen.
- Tkinter AVT and hydrotreatment buttons now open read-only stage dashboards with value/source/age/freshness instead of placeholders.
- Tkinter blend screen now has a hybrid sulfur-only panel that refuses to invent component A when history forecast is unavailable.

## Required evidence before full action capability

- Fresh canonical prepared dataset from current materials/runtime/tags/rules.
- Point, upper, and safety forecast artifacts trained from that canonical dataset.
- PAK and LIMS test reports written under `reports/`.
- Updated `config/model_freeze.json` after successful freeze verification.
- Action-effect gate report for `ht:P8` and `ht:F19`:
  - at least 100 change episodes per enabled control;
  - validation MAE at least 10% better than hold;
  - validation and audit upper coverage at least 95%;
  - stable effect sign across 3 temporal folds;
  - confirmed engineering units, bounds, step, and max_step;
  - shadow replay passed;
- technologist pilot approved.

## Финальное дополнение 21.09.2026

Ниже зафиксированы результаты именно после последних изменений, чтобы закрытые
записи не зависели только от зелёного общего теста.

### AP-004, AP-010, AP-013

- История и hybrid запускаются в worker-потоке; callback проверяет request ID,
  текущую страницу, существование виджета и живость окна.
- Настоящий Tk-сценарий с задержанным history-ответом и переходом в журнал дал
  `history_lifecycle=ok`; аналогичный hybrid-сценарий дал `hybrid_lifecycle=ok`.
- Отдельный delayed v2-dialog был закрыт до ответа worker и дал `dialog_lifecycle=ok`.
- Неверный timestamp `21.09.2026` возвращает завершённое состояние ошибки с
  исходной строкой времени; повторного падения в error-handler нет.

### AP-011 и AP-012

`config/release_manifest.json` закрепляет dataset `66bdfcbb23b4`, forecast
`sulfur-upper-33efba3c3141` и v2 `sulfur-v2-shadow-61f972d07181`. UI больше не
выбирает live action artifact, если manifest явно содержит `null`, и не использует
`[-1]`/mtime для pinned default. `scripts/build_release.ps1` собирает полный
комплект, исключает кэши, записывает `RELEASE_MANIFEST.json` и `SHA256SUMS.txt`.

### AP-014–AP-019

В `README.md`, `README_FIRST.md` и `RELEASE_BRIEF.md` добавлен буквальный Windows
маршрут без ручного поиска ID. Формы получили русские операторские названия,
общий dataset/time, точные доли рецептуры и массу партии. История поддерживает
`history-interval`; журнал показывает время, сценарий, статус и краткий след;
history/hybrid экспортируют именно текущий результат. Hybrid подписан как
условная sulfur-only оценка, v2 и action-study остаются shadow/research.

### AP-020–AP-022 и поставка

Финальный source of truth для release — `README_FIRST.md`, `RELEASE_BRIEF.md`,
`DEMO_CATALOG.json`, acceptance summary и pinned manifest. Старые DOCX/PPTX из
`output/jury-finalization-2026-09-18` не включаются в release и сохраняются как
архив, чтобы не смешивать старые commit/метрики с текущей поставкой.

Для каталога `output/финал release 2026-09-21-v9` проверено: ZIP содержит 148
элементов, настоящий RAR имеет magic `52 61 72 21`, LFS-pointer не найден,
архивный hash-check дал 143/143 совпадения. В новой папке с пробелами и кириллицей
прошли `validate-stage0`, model smoke, interval replay в lock-окружении и `pytest`
(`166 passed` для v5). Tk-навигация
прошла на `1120×760` и `1536×1024` без TclError. Полная проверка промышленной
метрики, независимой ML-оценки и action-capability по AP-005/AP-007/AP-008 этим
не закрывается.

### Повторное ревью изменений 21.09.2026

Повторно обнаружены и исправлены регрессии в закрытых пунктах:

- AP-011: отсутствующий pinned path приводил к выбору другого артефакта;
  теперь для явно заданных pin нет автоматической подмены. Регрессия покрыта
  `test_missing_release_pins_do_not_select_other_available_inputs`.
- AP-009/AP-018: после расчёта интервала кнопка экспорта сохраняла предыдущую
  точку. Теперь экспорт связан с завершённым видимым результатом; скрытый Tk-тест
  проверяет расчёт, уход/возврат на страницу и содержимое JSON.
- AP-009: полная загрузка dataset/model повторялась для каждой точки интервала.
  Теперь загрузка одна на запрос, временные проверки выполняются для каждой
  точки. Пустая sulfur-оценка не увеличивает forecast coverage.
- AP-012/AP-014: acceptance сборщика проверял checkout вместо копии, а README
  ссылался на не включённые документы. Проверка перенесена в каталог поставки,
  документы добавлены, fallback на старые отчёты удалён.

Подробности и границы повторной проверки: `FINAL_READINESS_REVIEW.md`, раздел
«Повторное ревью изменений». Прежний архив v9 не обновлялся этими правками.

AP-009 остаётся частично открытым по UX длительного расчёта: проверка текущей
поставки на двух реальных точках 01.06.2025 12:00–13:00 +03:00 заняла ~62 секунды.
Прогноз корректен, но поэтапного прогресса и отмены нет; переиспользование
dataset/model само по себе не закрывает отзывчивость этого пользовательского пути.

## Подтверждение финализации 22.09.2026

Дополнение к AP-012/014/020/021 — упаковка Git-репозитория: README разделяет готовый EXE и исходники, ведёт в Releases и к материалам жюри. Разработческий маршрут вынесен в docs/DEVELOPMENT.md, навигация по актуальным и историческим документам — в docs/README.md. CI проверяет Windows и Linux; большие бинарные ресурсы размещаются в релизе, не в обычной истории Git. Публикуемая версия должна иметь точный тег, SHA-256 и успешный portable-smoke.

AP-021, найдено при публикации: тест выбора release-контекста зависел от игнорируемых данных/моделей на машине автора. На чистых GitHub runners Windows и Linux получалось 172 pass / 1 fail. Тесты выбора контекста переведены на временные metadata-fixtures; отсутствие закреплённых ресурсов по-прежнему даёт пустой контекст, а не случайную модель. Проверка настоящих данных и весов остаётся в portable-smoke готовой поставки.

AP-004/010: фоновые задачи передают callback через очередь, Tcl вызывается главным потоком. Последние значения по источникам заменили полную историю в ProcessState; временные фильтры и выбор валидных данных сохранены. Поиск опубликованного качества в признаках векторизован. На текущем ПК реальный двухточечный интервал ускорился с 61,99 до 16,83 с; семь точек — 47,99 с. Это измерения, не гарантия скорости любого ПК.

AP-009/018: отмена на границе точек, progress и явный cancelled; завершённые точки сохраняются и экспортируются. UI-регрессия проверяет переход и экспорт. Ошибка смешения pack/grid исправлена; исключения приложения больше не маскируются как отсутствие Tk-дисплея.

AP-005/008: validation финального переобученного artifact маркируется in_sample_diagnostic. Отложенный test 2026: 31 819 сопоставимых валидных целей, MAE 0,6842 мг/кг, равен persistence baseline. Это закрывает пробел проверки, но не заявляет превосходства модели. Полный отчёт: release/evidence/forecast-test/metrics.json.

AP-007: blend_tradeoff имеет два допустимых варианта; выбран меньший риск 0,80 → 0,73 при большей стоимости 1,99 → 2,0395. Выпуск остаётся фиксированным. Код и evidence проверяют именно конфликт критериев.

AP-020: release/jury содержит новые материалы на данных этой ревизии. Все 12 слайдов и 7 страниц визуально проверены, таблицы редактируемые. README_FIRST и DEMO_SCRIPT задают буквальный маршрут. DESIGN дополнен фактическими контрактами снимка и очереди UI.

Финальный исходный код: 173 pytest-теста без пропусков, Ruff и mypy (35 файлов) проходят. Проверка переносимой сборки сохраняется внутри поставки в reports/portable-smoke.json. Промышленные ограничения, перечисленные в RELEASE_STATUS, не являются скрыто реализованными возможностями.

## Независимое ревью готовности к сдаче 22.09.2026

Область: локальный `NEFTEKOD_FINAL_2026-09-22.zip`, исходники и состояние GitHub.
Во время ревью HEAD/main/dev обновились с `1aba5e8` до `7048934`; новый коммит
меняет тестовые fixtures и журнал, но не приложение. Повторный локальный запуск
на `7048934`: 173 passed, без пропусков; Ruff/format и mypy (35 файлов) проходят.
Это не подтверждение результата ещё выполняющегося удалённого CI.

Дополнение AP-012/021: ZIP проверен штатным `scripts/verify_release.py` — 3252
файла совпали с SHA256SUMS. Архив распакован в новую папку
`D:\ML\hakneft\tmp\jury-review-20260922-extracted`; EXE запущен с PATH,
содержащим только Windows/System32 и Windows. Portable-smoke завершился кодом 0:
шесть ожидаемых demo-статусов, две исторические точки, Tk ok, hybrid/АВТ/
гидроочистка ready, v2 shadow_only и supports_actions=false. Новый отчёт:
`D:\ML\hakneft\tmp\jury-review-20260922-portable.json`.
Это новая папка на том же ПК, а не другая ОС или физически отключённая сеть.
Manifest проверенного локального архива честно содержит source_tree_dirty=true
и source_commit=88e317e; для однозначной воспроизводимости финальной публикации
нужна сборка из чистого закреплённого commit. Контрольная сумма ZIP:
`84445e77e551e6c582f6aa0516c7ba0379821f2f9ddafb341e0c83cf781292e8`.

### AP-023. Нет пользовательского what-if редактора модельной смеси

- Приоритет: P2. Тип: подтверждённое ограничение, не ошибка вычислений.
- Код: `source/ui.py`, `_render_overview()` и `calculate()`;
  `config/scenarios/*.json`, `run_model_demo()`.
- Воспроизведение: открыть «Обзор»; доступны readonly-выбор сценария и единственный
  горизонт 60 минут, но нет полей свойств компонентов, запасов и дозы присадки.
- Наблюдение: движок рассчитывает выбранную конфигурацию, однако произвольный
  вопрос жюри требует редактирования JSON вне интерфейса. Выбор другого времени
  в history меняет исторический прогноз, но не является what-if рецептурой.
- Ожидание: либо явно позиционировать фиксированное сценарное демо, либо дать
  ограниченную синтетическую форму с валидацией, пересчётом и сбросом к пресету.
- Критерий закрытия: проверенный ввод меняет вычисленный результат; невалидные
  доли/неполные свойства не дают допустимую рекомендацию; маркировка synthetic
  и запрет реальных действий сохранены. Реализация в рамках ревью не выполнялась.

### AP-024. GitHub-поставка ещё не доступна как готовый выпуск

Обновление 22.09: первоначальное наблюдение ниже относится к 01:07 МСК.
v1.0.0 уже опубликован с полным ZIP; новая версия v1.1.0 подготовлена
из чистого main f4892d7, CI Windows/Linux прошёл. Доступ конкретной учётки
жюри не проверен, репозиторий остаётся закрытым; это отдельная граница доставки.

- Приоритет: P1 при сдаче только GitHub-ссылкой. Не блокирует передачу уже
  проверенного локального ZIP непосредственно жюри.
- Источник: авторизованные read-only GitHub API запросы 22.09.2026 около 01:07 МСК.
- Репозиторий private=true; `/releases/latest` возвращает 404. Список releases
  содержит v1.0.0 с draft=true, published_at=null и двумя assets: DOCX/PPTX.
  Готового `Neftekod-Windows-x64.zip` в этом снимке draft нет.
- README ведёт пользователя в Releases за EXE; обычный Code/Download ZIP
  содержит исходники, а не полный переносимый runtime с данными и моделями.
- Предыдущий удалённый CI на 1aba5e8 завершился failure на pytest Windows/Linux;
  исправление fixtures входит в 7048934, CI нового коммита на момент проверки
  ещё in_progress. Локально исправленный коммит проверен успешно.
- Критерий закрытия: получить зелёный CI закреплённого коммита, опубликовать
  проверенный runtime-ZIP с checksum и подтвердить доступ именно учётки жюри
  либо передать полный ZIP другим согласованным каналом. Публикация/изменение
  доступа в рамках этого ревью не выполнялись; состояние может измениться
  параллельно, поэтому при закрытии нужна повторная проверка.

Повторная проверка 01:08 МСК: один CI-run на 7048934 уже завершился success,
второй ещё in_progress. Это обновляет промежуточный статус выше; v1.0.0 остаётся
draft с двумя DOCX/PPTX assets, без runtime-ZIP.

## Дополнение к AP-015 и AP-020 22.09.2026

По запросу пользователя устранён локальный пробел пользовательской документации:
существующий DEMO_SCRIPT дополнен полным руководством по шести разделам UI,
дополнительным окнам, общим dataset/time, фиксированным сценариям, трём экспортам,
статусам и ожидаемым отказам. В main это `docs/DEMO_SCRIPT.md`; в dev обновлён
существующий `release/DEMO_SCRIPT.md`. Новые MD в репозитории не создавались.

Локальный `release/jury/Neftekod_Jury.docx` содержит 13 страниц, руководство занимает
страницы 8–13. Технические абзацы и таблицы сохранены; заменена старая навигация,
включая неверное расширение `residuals.csv` на фактическое `residuals.csv.gz`.
Проверены 31 уникальная подпись кнопок, полей и раскрываемых блоков из кода,
совпадение руководства в Markdown и Word, локальные ссылки и якоря; все 13 страниц
последней версии визуально проверены после экспорта через Word. Код UI не менялся.

Граница завершения: изменения документации локальные, не опубликованы в GitHub;
существующий релиз v1.0.0 и его ZIP не изменялись. Для доставки обновлённого Word
через Releases требуется новая версия, а не перезапись старого выпуска. AP-023
не закрывается этим дополнением: произвольный редактор рецептуры не реализован,
его отсутствие теперь явно описано для жюри. Предыдущие записи ревью сохранены.

## Закрытие AP-023 и обновление UI 22.09.2026

По следующему запросу пользователя реализован `source/ui_what_if.py`:
проверяемая копия ScenarioConfig, свойства A/B, запасы, доли, партия, стоимость,
риск и синтетическая присадка. Пересчёт использует прежний run_cycle; исходный
preset не меняется. Сброс восстанавливает поля с явным пересчётом. Изменённый
ввод сохраняется в input.json и synthetic_scenario экспортируемого JSON.
Невалидный ввод не применяется; нехватка ресурсов/качества даёт abstain.

История показывает point/upper и лимит 10 мг/кг на графике; время цели равно
as_of + 60 минут, разрывы данных не соединяются. Пути скрыты в дополнительных
настройках. Управляющий контур не расширен: supports_actions=false.

Проверка main f4892d7: 194 pytest без пропусков, Ruff, mypy (37 файлов).
21 новый тест включает пересчёт, сброс, неизменность preset, журнал, валидацию,
отказы, Tk-редактор и график с пропусками. Portable EXE из нового ZIP проверен
при PATH без Python: what_if recommend, editor ok, history_chart 2 точки,
rendered=true, Tk ok. Проверены SHA-256 всех 3238 файлов архива; manifest
source_tree_dirty=false. Готовые документы: Word 14 страниц, PPTX 14 слайдов,
36 подписей UI покрыты инструкцией; рендеры просмотрены. Исторические записи
об отсутствии редактора выше сохранены и заменены этим статусом.

## Повторная оценка системы 22.09.2026, 12:52 МСК

- Проверены dev `f95ef4b` и опубликованный main/tag v1.1.0 `f4892d7`.
  Различий source/config/global_tests между ними нет. Свежие локальные проверки:
  194 pytest без пропусков, Ruff check/format (92 файла), mypy (37 файлов) — pass.
- AP-023 подтверждён закрытым: тесты воспроизводят изменение рассчитанной серы,
  восстановление результата после сброса, неизменность preset, журналирование
  входов, отклонение неверных чисел/долей и abstain при недостающих ресурсах.
  Проверены код редактора и регрессия настоящего Tk. What-if остаётся synthetic.
- AP-024: авторизованный GitHub API подтверждает v1.1.0 draft=false, полный
  runtime-ZIP, DOCX/PPTX и checksum; CI main и dev — completed/success.
  Репозиторий private=true, доступ конкретной учётки жюри не проверялся.
- SHA-256 локального `output/v1.1.0-release/Neftekod-Windows-x64.zip` совпал
  с digest опубликованного asset:
  `db723119671a6127f5766a49d36f65b88ce343b5decaf27af0ec0a0249bb866f`.
  Штатный verify_release повторно подтвердил все 3238 файлов; manifest содержит
  source_tree_dirty=false и source_commit=f4892d7. Файлы source/config/global_tests
  в архиве совпали с текущими локальными файлами после нормализации CRLF.
- Существующий отчёт portable-smoke внутри проверенного ZIP содержит Tk ok,
  what_if recommend/editor ok, history_chart 2 точки/rendered=true и шесть
  ожидаемых сценарных статусов, без error. EXE в этом повторном ревью заново
  не запускался: использована проверенная evidence той же сборки.
- AP-008/AP-019 остаются содержательными ограничениями: модели закреплены
  прежние, MAE основного прогноза 0,6842 равен baseline; v2 audit_gate_passed=false
  и promotion_eligible=false. Hybrid остаётся sulfur-only, действия отключены.
- Остаточный UX-пробел AP-015: история стала нагляднее и скрывает технические
  пути, но ввод времени остаётся ISO-строкой. `source/ui_charts.py` рисует только
  point/upper/порог, не фактическое качество на момент цели. Это ограничение
  сравнения прогноза с исходом, не ошибка существующего графика. Возможное
  улучшение — отдельная явно подписанная ретроспективная серия факта без её
  использования как будущего признака модели. В рамках ревью не реализовано.

Новых блокирующих регрессий в проверенном объёме не выявлено. Повторная оценка
не заменяет запуск полученного архива на отдельном Windows-компьютере жюри.
