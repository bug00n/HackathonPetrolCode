# Нефтекод — brief для защиты

Обновление 22.09.2026: основной актуальный текст — `JURY_REPORT.md`, маршрут показа — `DEMO_SCRIPT.md`. Готовый Windows-комплект запускается через `Neftekod.exe` без установки Python. К пяти основным эпизодам добавлен шестой `blend_tradeoff`: риск 0,80 → 0,73 при стоимости 1,99 → 2,0395. Независимый test на 31 819 целях ПАК: MAE 0,6842 мг/кг, равен persistence baseline; превосходство не заявляется.

## Версия и границы

Release ID: `neftekod-dev-2026-09-21`.

Поставка локальная и read-only. Она показывает модельные рецептуры, исторический
прогноз серы и исследовательские отчёты. `supports_actions=false`: реальные
уставки, команды оборудованию и производственная экономия не заявляются.

Вся демонстрация использует pinned-контекст из `config/release_manifest.json`:

- prepared dataset: `66bdfcbb23b4`;
- forecast: `sulfur-upper-33efba3c3141`;
- v2: `sulfur-v2-shadow-61f972d07181`;
- action artifact: отсутствует в live-контуре.

## Что показывать

Пять модельных эпизодов из `reports/release-acceptance/summary.json`:

| Эпизод | Результат |
| --- | --- |
| `blend_normal` | `hold` |
| `blend_risk` | `recommend` |
| `blend_t95_risk` | `recommend` |
| `blend_cetane_risk` | `recommend` |
| `blend_missing` | `abstain`, неизвестная сера не превращается в ноль |

Для исторического показа используйте `2025-06-01T12:00:00+03:00` и команду
`history-interval` из `README_FIRST.md`. Она возвращает выбранные точки,
покрытие, прогноз point/upper, отказы и ограничения; результат остаётся
forecast-only.

Hybrid — условное смешение только по прогнозу серы. Оно не является полным
товарным паспортом и не доказывает оптимизацию всей цепочки АВТ → гидроочистка →
смесь. v2 и action-study показываются только как shadow/research evidence.

## Числа и доказательства

Источник чисел — acceptance summary, журналы и pinned artifacts внутри этой
поставки. `SHA256SUMS.txt` проверяет файлы архива. Старые презентации и описания
из `output/jury-finalization-2026-09-18` не являются частью release и должны
рассматриваться как архивные материалы, а не как текущая версия продукта.
